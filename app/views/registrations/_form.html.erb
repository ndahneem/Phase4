<%= form_with(model: registration, local: true) do |form| %>
  <% if registration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(registration.errors.count, "error") %> prohibited this registration from being saved:</h2>

      <ul>
      <% registration.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :camp_id %>
    <%= form.text_field :camp_id, id: :registration_camp_id %>
  </div>

  <div class="field">
    <%= form.label :student_id %>
    <%= form.text_field :student_id, id: :registration_student_id %>
  </div>

  <div class="field">
    <%= form.label :payment %>
    <%= form.text_area :payment, id: :registration_payment %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
